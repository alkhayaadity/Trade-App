"use client";
import {Area,AreaChart,CartesianGrid,Cell,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from "recharts";
import type {Trade} from "@/types";
export function EquityChart({trades,initial=100000}:{trades:Trade[];initial?:number}){const data=trades.map((t,i)=>({name:i+1,balance:initial+trades.slice(0,i+1).reduce((sum,row)=>sum+row.pnl,0),date:t.date.slice(5)}));return <div className="h-[285px]"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{top:10,right:10,left:0,bottom:0}}><defs><linearGradient id="eq" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#4F7CFF" stopOpacity={.35}/><stop offset="1" stopColor="#4F7CFF" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="#242A35" strokeDasharray="3 6" vertical={false}/><XAxis dataKey="date" axisLine={false} tickLine={false} tick={{fill:"#697386",fontSize:10}} minTickGap={28}/><YAxis axisLine={false} tickLine={false} tick={{fill:"#697386",fontSize:10}} tickFormatter={v=>`$${Math.round(v/1000)}k`}/><Tooltip contentStyle={{background:"#111720",border:"1px solid #2A3240",borderRadius:12}}/><Area type="monotone" dataKey="balance" stroke="#6E93FF" strokeWidth={2.3} fill="url(#eq)"/></AreaChart></ResponsiveContainer></div>}
export function WinLoss({trades}:{trades:Trade[]}){const data=[{name:"Wins",value:trades.filter(t=>t.status==="win").length,color:"#16C784"},{name:"Losses",value:trades.filter(t=>t.status==="loss").length,color:"#EA3943"},{name:"B/E",value:trades.filter(t=>t.status==="breakeven").length,color:"#647084"}];return <div className="relative h-[260px]"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" innerRadius={65} outerRadius={86} paddingAngle={4} stroke="none">{data.map(x=><Cell key={x.name} fill={x.color}/>)}</Pie><Tooltip contentStyle={{background:"#111720",border:"1px solid #2A3240",borderRadius:12}}/></PieChart></ResponsiveContainer><div className="pointer-events-none absolute inset-0 grid place-items-center text-center"><div><b className="text-2xl">{trades.length}</b><span className="block text-[10px] text-muted">Trades</span></div></div></div>}
